import {createSlice} from "@reduxjs/toolkit"

const payment_action = createSlice({
        name: "payment",
        initialState : {


        },

        reducers: {



        }
    }
);

export const {

} = payment_action.actions;

export default payment_action.reducer;